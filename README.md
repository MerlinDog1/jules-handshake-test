# Jules Handshake Test

This is a clean repository to test the integration between **Teto** (OpenClaw) and **Jules** (Google Labs).

## Setup
Jules should refer to `.jules/setup.sh` and `AGENTS.md` for context.
